spring:
  application:
    name: kryxhub
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/kryxhub_db
    username: kryxhub_admin
    password: arcticocean
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true